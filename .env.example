# Example environment variables for this Vite React app
# Copy this file to `.env` and fill values (do NOT commit your real `.env`).

# Base URL for backend API (used by services). Example: http://api.example.com
VITE_API_URL=http://localhost:3000

# Optional: endpoint path for sending notas via backend (if used)
VITE_SEND_NOTA_PATH=/send-nota

# Optional WhatsApp gateway base URL (if separate from API)
VITE_WHATSAPP_API_URL=http://localhost:3000

# Application name
VITE_APP_NAME=ZEA_TEXTILE

# Optional third-party keys
VITE_GOOGLE_MAPS_API_KEY=
VITE_SENTRY_DSN=

# Notes:
# - This project uses Vite; environment variables available in client code must start with `VITE_`.
# - Some services currently read localhost values directly; you can refactor to use `import.meta.env.VITE_API_URL`.
